ARGUS_LLM_BASE_URL=https://llm.example.com
ARGUS_LLM_API_KEY=replace-me
ARGUS_LLM_MODEL=model-name
# Optional: HTTP timeout in seconds for the LLM client. Default: 600
# (reasoning models like deepseek-v4-pro need more than the previous 120s hard limit).
# ARGUS_LLM_TIMEOUT=600
